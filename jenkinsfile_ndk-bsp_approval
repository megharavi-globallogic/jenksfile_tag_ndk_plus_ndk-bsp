pipeline {
	agent none
	
	parameters {
    		string(name: 'VERSION_RELEASE',defaultValue: '')
  	}
	
	options {
        timeout(time: 7, unit: 'DAYS')
    	}
	
    stages {
    	stage("Set version name"){
        	steps {
                	script {
                    		currentBuild.displayName = "${params.VERSION_RELEASE}"
                	}
            	}
        }
    }
	
}
